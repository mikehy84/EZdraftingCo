
@model List<PersonDto>

    
<h1>Person List</h1>
<table class="table">
    <thead>
        <tr>
            <th>Name</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var person in Model)
        {
            <tr>
                <td>@person.Name</td>
                @* <td>
                    <a asp-area="Admin" asp-controller="Person" asp-action="Edit" asp-route-id="@person.Id" class="btn btn-primary">Edit</a>
                    <a asp-area="Admin" asp-controller="Person" asp-action="Delete" asp-route-id="@person.Id" class="btn btn-danger">Delete</a>
                </td> *@
            </tr>
        }
    </tbody>

</table>