@model List<PriorityDto>

<div>
    <h2 style="color:aliceblue">Priority List</h2>
    <table style="color:aliceblue">
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Description</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var priority in Model)
            {
                <tr>
                    <td>@priority.Id</td>
                    <td>@priority.Name</td>
                    <td>@priority.Definition</td>
                </tr>
            }
        </tbody>
    </table>
</div>
